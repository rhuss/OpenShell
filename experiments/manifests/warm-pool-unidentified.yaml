apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: openshell-grpc-pool
  namespace: openshell
spec:
  sandboxTemplateRef:
    name: openshell-warm-unidentified
  replicas: 3
